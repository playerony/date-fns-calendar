import { functionImportTest } from '@amperio/amperio-ui';

import { {{camelCase name}} } from './{{kebabCase name}}.hook';

describe('{{camelCase name}} hook', () => {
  functionImportTest({{camelCase name}});

  it('', () => {
    
  })
});
