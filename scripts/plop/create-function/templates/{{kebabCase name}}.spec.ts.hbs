import { functionImportTest } from '@amperio/amperio-ui';

import { {{camelCase name}} } from './{{kebabCase name}}.function';

describe('{{camelCase name}} function', () => {
  functionImportTest({{camelCase name}});

  it('', () => {
    
  })
});
